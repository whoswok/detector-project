[build]
builder = "dockerfile"

[deploy]
startCommand = "echo 'Detector Project - Deploy via Railway\\n\\nAccess your live demo at the provided URL!'"

[[services]]
name = "detector-app"
type = "web"
